import { StackProps } from "@mui/material";

export const statusDotContainerStyles = (size: number = 12): StackProps => ({
  height: `${size}px`,
  width: `${size}px`,
  borderRadius: "50%",
  marginTop: "4px",
});
