import { StackProps } from "@mui/material";

export const authContainerStyles: StackProps = {
  margin: "auto",
  width: "100%",
  maxWidth: 1300,
};

export const authContainerContentStyles: StackProps = {
  direction: "row",
  justifyContent: "space-between",
  alignItems: "center",
  padding: 3,
  gap: 4,
};
